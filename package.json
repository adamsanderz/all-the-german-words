{
  "name": "all-the-german-words",
  "version": "1.0.2",
  "description": "An array of ~168,000 German words. Offers both correct spelling and all lowercase. Works in node and browsers.",
  "main": "index.js",
  "bin": {
    "woerter": "./cli.js",
    "all-the-german-words": "./cli.js"
  },
  "repository": "https://github.com/creativecouple/all-the-german-words",
  "author": "CreativeCouple, zeke",
  "license": "MIT",
  "scripts": {
    "create-corpus": "node crawl-wiktionary.js > corpus/wiktionary.txt && node fetch-atebits.js > corpus/de.txt",
    "build": "node build.js > woerter.json && node build.js -l > lowercase.json",
    "test": "npm run build && mocha && standard"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "lodash": "^4.17.2",
    "mocha": "^3.2.0",
    "readlines": "^0.2.0",
    "standard": "^8.6.0"
  },
  "engines": {
    "node": ">=4"
  },
  "standard": {
    "env": {
      "mocha": true
    }
  }
}
